name: Validate terraform modules

on:
  push:
    branches:
      - main
    paths:
      - "terraform/modules/**"
      - ".github/workflows/action-validate-tf-module.yml"
  pull_request:
    paths:
      - "terraform/modules/**"
      - ".github/workflows/action-validate-tf-module.yml"

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        directory:
          [
            "terraform/modules/block-az-analytics",
            "terraform/modules/block-az-kv",
            "terraform/modules/block-az-nsg",
            "terraform/modules/block-az-rg",
            "terraform/modules/block-az-routetable",
            "terraform/modules/block-az-vnet",
            "terraform/modules/block-core-convention",
          ]
    defaults:
      run:
        working-directory: ${{ matrix.directory }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Terrascan
        uses: ./github/actions/terrascan
        with:
          working-directory: ${{ matrix.directory }}
          scan-type: "terraform"
          severity: "HIGH"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_wrapper: false

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate
