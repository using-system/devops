skip-check:
  - CKV2_AZURE_32 # "Ensure private endpoint is configured to key vault" --> pep configured outside
  - CKV_AZURE_109 # "Ensure that key vault allows firewall rules settings" --> allow to set a private key vault withour Azure Services
